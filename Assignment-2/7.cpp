#include <iostream>
#include <math.h>
using namespace std;
int main(){
    int a,b;
    cin>>a>>b;
    double distance;
    distance = sqrt(pow(a,2)+ pow(b,2));
    cout<<distance<<endl;
    return 0;
}
